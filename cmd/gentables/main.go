// Command gentables converts the libopus static mode data (static_modes_fixed.h)
// and SILK tables into checked-in Go data. The generated files record the
// upstream tag and generation method in their header. See docs/hard-parts.md 6.
package main

func main() {
	// TODO(phase 0/2): generate internal/celt and internal/silk table data.
}
